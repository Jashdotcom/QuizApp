# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/quiz_app?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Jash_2006

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Remove Security Auto-configuration (since you have custom security)
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Server Port
server.port=8080

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# JWT Configuration
quizapp.jwtSecret=QuizAppSpringBootSecretKey1234567890
quizapp.jwtExpirationMs=86400000

# Debug SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Show registration debug info
logging.level.com.example.quizzapp.controller=DEBUG

# Email Configuration (Comment out if not using email)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-email@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true

# For development/testing without real email
spring.mail.host=localhost
spring.mail.port=1025

# Password Encoder (This property doesn't exist - remove it)
# spring.security.encoding.strength=10

# Spring Security (Add these for better security configuration)
spring.security.filter.order=10
# Drop and recreate the database
# or use:
# Show SQL for debugging
# Change this line to create or create-drop

# This will recreate your database with the new schema

